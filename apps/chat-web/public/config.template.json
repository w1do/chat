{
  "apiBaseUrl": "${API_BASE_URL}",
  "reverb": {
    "host": "${REVERB_HOST}",
    "port": "${REVERB_PORT}",
    "scheme": "${REVERB_SCHEME}",
    "appKey": "${REVERB_APP_KEY}"
  },
  "ai": {
    "enabled": "${AI_ENABLED}"
  },
  "push": {
    "publicKey": "${VAPID_PUBLIC_KEY}"
  },
  "branding": {
    "appName": "${APP_NAME}"
  }
}
