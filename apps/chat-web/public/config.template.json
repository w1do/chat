{
  "apiBaseUrl": "${API_BASE_URL}",
  "reverb": {
    "host": "${REVERB_HOST}",
    "port": "${REVERB_PORT}",
    "scheme": "${REVERB_SCHEME}",
    "appKey": "${REVERB_APP_KEY}"
  },
  "ai": {
    "enabled": "${AI_ENABLED}"
  },
  "auth": {
    "silentRecovery": "${AUTH_SILENT_RECOVERY}"
  },
  "push": {
    "publicKey": "${VAPID_PUBLIC_KEY}"
  },
  "password": {
    "minLength": "${PASSWORD_MIN_LENGTH}"
  },
  "branding": {
    "appName": "${APP_NAME}"
  }
}
