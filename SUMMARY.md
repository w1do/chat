# SUMMARY — текущее состояние проекта

Честное состояние на 2026-08-27. Статусы: `planned` / `in progress` /
`implemented` / `verified`. Статус `implemented` ставится только при наличии
кода и тестов в репозитории.

## Что сделано

Roadmap `openspec/changes/rebuild-chat-by-structure` пройден полностью
(этапы 1–14). Продукт собирается из пакетов: composition roots `apps/chat-api`
(Laravel 13, Octane/FrankenPHP, Pest) и `apps/chat-web` (React 19, Vite,
TypeScript strict), шесть backend-пакетов и шесть frontend-пакетов,
`packages/contracts` с JSON Schema real-time событий, CLI `./tools/chat`,
`infra/` (Docker, Compose, Supervisor), документация и ADR-001…010.

Полный локальный прогон CI — `./tools/chat ci`; критические сценарии —
`./tools/chat e2e critical`; готовность развёрнутого стека — `./tools/chat
smoke all`.

## Интерфейс

Продуктовый UI — мобильный: вкладки «Чаты»/«Настройки», лента с группировкой
по автору, ответы, реакции, упоминания, индикатор набора и состояние
соединения, светлая/тёмная тема. Детали и соответствие домену —
[docs/features/mobile-ui.md](docs/features/mobile-ui.md).

## Модули

| Модуль | Что сделано | Статус |
|---|---|---|
| platform/monorepo-foundation | Каркас монорепозитория и границы пакетов: `check boundaries` ловит cross-package internals, deep imports, расхождение сгенерированных файлов и `Domain/` в apps, `check docs` — статусы документации, `check selftest` доказывает, что обе проверки падают на подложенном нарушении; workflows pull-request/security/release/deploy-*; `./tools/chat ci` повторяет их локально | verified |
| operations/self-hosted-runtime | Финальный гейт: production-профиль поднят `docker compose up -d`, миграции применены, `smoke all` (runtime + websocket + storage + search) зелёный; ADR-007/009/011; compose prod/dev (`compose config` ок); образы api (FrankenPHP+Octane+Horizon+Reverb) и web собираются (`build images` ок); Supervisor-конфиги (`supervisor check` ок); readiness `/api/v1/readiness` + `/up` (в компонентах — и storage); `smoke runtime` против dev-стека — все компоненты ok; объектное хранилище обязательно (ADR-011): диск `media` поверх бакета `chat` с раскладкой по видам, бакет создаётся при старте идемпотентно, `smoke storage` пишет и читает пробный объект; medialibrary с webp-конверсиями в очереди `media`; graceful reload (octane/horizon/reverb) проверен в контейнерах | verified |
| contracts/api-and-realtime | Единый error envelope + `X-Trace-Id` (9 feature-тестов: validation/401/403/404/409/429/500); сборка OpenAPI из фрагментов пакетов в коммитящийся `dist/openapi.json` (`openapi validate` ок); генерация `api-client` из dist (`client generate` + typecheck ок) с обёрткой 401/419/429; финальные JSON Schema 6 событий + `RealtimeSchemaTest` (8 тестов) | implemented |
| identity/authentication-and-profile | E2E `e2e auth`; вход по логину без почты (почта и пароль — в настройках), полный срез регистрации/входа/выхода/восстановления/профиля (19 package-тестов), Sanctum SPA (ADR-005) c CORS/CSRF/rate-limit интеграционными тестами (8), Octane leak-тесты + `smoke octane` под FrankenPHP, OpenAPI+client, формы на дизайн-системе и guard'ы во frontend; требование к паролю одно на сервер и интерфейс (`PASSWORD_MIN_LENGTH` через `/config.json`), ошибки форм — человеческим текстом, а не служебными кодами, введённый пароль можно показать (кнопка в поле, без автозамены, со скрытием при отправке), фокус полей виден; истёкшая сессия разбирается в одном месте — первый 401 после входа открывает один инцидент, повторы и сокет гасятся, поверх чата встаёт неснимаемый экран «Сессия истекла» с выходом и перезагрузкой, тихое восстановление (одна попытка) включается `AUTH_SILENT_RECOVERY`; 19 компонентных и интеграционных тестов chat-web | verified |
| notifications/web-push | Web Push с VAPID: подписки устройств, доставка при закрытом приложении, удаление аннулированных подписок, повтор при сбое push-сервиса; тумблер в настройках. Без ключей выключено. 33 теста пакета, 7 интеграционных | implemented |
| platform/installable-app | Приложение ставится на телефон и компьютер: манифест, иконки, service worker с push и экраном «Нет связи», обновление без переустановки, подсказка об установке | implemented |
| platform/image-cache | Кеш изображений на устройстве: миниатюры вложений и картинки профилей и комнат в `chat-images-v1` по стратегии «сначала кеш», LRU-вытеснение сверх 400 записей, отказ кешировать ошибки и ответы больше 2 МБ, устойчивость к отказу хранилища; фоновый прогрев ближайших изображений (параллельность 3, старт по простою, без повторов) и полная очистка при выходе, истёкшей сессии и смене человека в `/me`. 8 тестов кеша в service worker'е, 4 теста очистки в приложении, 1 в потоке истёкшей сессии, 4 теста прогрева | implemented |
| platform/app-shell | Оболочка владеет экраном: страница не прокручивается и не отскакивает, каждый экран — `Screen` с закреплёнными краями и одной прокручиваемой серединой; панель ввода видна целиком: нижний отступ считается по реальной видимой области (`--app-bottom-inset`), а не по одной безопасной зоне, и долгое нажатие не запускает системное выделение. Жестом приложение не масштабируется (`touch-action: pan-x pan-y` плюс подавление `gesture*` для iOS Safari), при этом увеличение из меню браузера, системное принудительное увеличение и размер текста в настройках остаются — WCAG 2.2 AA (1.4.4) соблюдён. Увеличенная страница не принимается за клавиатуру, при ней высота берётся по layout-вьюпорту; геометрия перемеряется по возврату из фона (`visibilitychange`/`pageshow`/`focus`, серия измерений примерно до секунды), под оболочкой закрашен фон темы. Проверено компонентными тестами и E2E (`window.scrollY` остаётся нулём; `e2e mobile-zoom` — двойной тап и возврат из фона); проверка на живых iPhone и Android, включая возврат по уведомлению, — открытый пункт | implemented |
| platform/desktop-layout | Две колонки от 1024 px (список комнат и переписка), выбор раскладки по ширине окна; E2E `e2e desktop` | implemented |
| chat/message-interaction | Действия над сообщением жестами: свайп влево — ответ, двойное касание — `❤️`, долгое нажатие и правый клик — меню (палитра, ответить, копировать, удалить); всё дублируется клавиатурой. Постоянных кнопок в ленте нет. Правка своего текста идёт из меню в строку ввода: плашка с исходным сообщением, отмена по Escape, метка «изменено» в ленте и мгновенное `message.updated.v1` у остальных. Ввод `@` открывает меню участников с аватаркой, присутствием и ником — стрелки, Enter/Tab и Escape; в ленте упоминания рисуются бейджами, сообщение с упоминанием читающего помечено полосой. 175 компонентных тестов пакета chat | implemented |
| chat/visual-language | Лента по макету (аватар и имя с обеих сторон, разделитель дня), «печатает» в шапке со сворачиванием, экран приложения заякорен к `visualViewport` (клавиатура на iOS не уводит экран), центрированные формы входа | implemented |
| chat/invite-links | Ссылка-приглашение в комнату: создаёт участник, живёт 7 дней, многоразовая, отзывается; в базе только хэш токена, ограничения по частоте. Кнопка «Пригласить» и экран `/invite/{token}`. 9 package-тестов, 8 интеграционных, 6 компонентных, E2E из двух браузеров | implemented |
| identity/profile-images | Аватарка профиля с набором прежних (выбор текущей — перевод указателя, набор виден только владельцу), фотография комнаты (owner/admin, заменяет эмодзи), личные обои переписки с подложкой для читаемости. Только webp двух размеров, исходник не хранится; файлы отдаёт приложение из закрытого бакета. 15 package-тестов identity, 6 chat, 10 feature, 12 компонентных; E2E `e2e profile-images` | verified |
| identity/guest-accounts | Аккаунт по приглашению: имя называет человек, логин и пароль генерируются, подсказка в настройках, пока пароль не задан | implemented |
| chat/rooms-and-messages | Полный срез: комнаты/членство (матрица ролей, единственный owner в БД; приглашение по нику через `GET /rooms/{room}/member-candidates` и исключение участника через `DELETE /rooms/{room}/members/{member}` — владелец убирает любого кроме себя, админ только обычного участника, событие `room.member_changed.v1` с `action: removed` уводит исключённого к списку комнат; владелец правит название и описание и удаляет комнату навсегда одной транзакцией с записью в аудит, архивирование — отдельным адресом `POST /rooms/{room}/archive`, событие `room.deleted.v1` уводит читающих к списку комнат) + сообщения/ответы/реакции/упоминания (санитизация, cursor-пагинация, окно редактирования, soft delete с ответами, идемпотентная отправка, unread-счётчики). 53 package-теста, 49 feature-тестов, 93 компонентных; OpenAPI 16 путей + клиент; UI: история, композер с reply/mentions, реакции с optimistic rollback; E2E `e2e messaging` и `e2e membership` | verified |
| chat/attachments | Вложения к сообщениям: загрузка отдельным запросом в границах комнаты (белый список типов по расширению и содержимому, запрет исполняемого, пределы размера/количества/частоты из конфигурации), сообщение без текста при наличии вложений, файлы только в бакете под `uploads/` с выдачей через приложение по праву чтения сообщения (`Content-Disposition: attachment`, 404 постороннему/исключённому/у удалённого), webp-миниатюра снимка в пределах `ATTACHMENT_PREVIEW_SYNC_MAX_KB` (4096 по умолчанию) готовится прямо в запросе загрузки, файл крупнее — в очереди `media`, уборка неотправленного по расписанию и файлов комнаты при её удалении; UI: скрепка с предпросмотром и повтором, плитки (1 крупно / 2–4 сеткой / «Показать ещё»), галерея со свайпом и клавиатурой, плитки из события без перезапроса, догоняющий запрос отложенной миниатюры (до трёх попыток на сообщение, затем честное «недоступна» с открытием оригинала). 17 package-тестов, 13 feature, 22 компонентных; `message.created.v1` дополнено вложениями без смены версии; E2E `e2e attachments` | verified |
| chat/realtime-presence | Системные записи о членстве в истории, конфетти-приветствие, уведомления о входящих (бейджи, заголовок вкладки, тост, Notification API). Broadcast после commit (6 событий, payload по схемам contracts), авторизация приватных/presence/user каналов, Redis presence-реестр с TTL, typing endpoint; фронт: EchoAdapter, `useRealtimeRoom` с HTTP-ресинком после reconnect, TypingIndicator/PresenceDots. Присутствие человека: метка `users.last_seen_at` двигается любым его действием (запись троттлится окном), `is_online`/`last_seen_at` отдаются в профиле, составе комнаты и у собеседника диалога, интерфейс подписывает «В сети» и «был(а) в сети …». Проверено: 7 integration + 4 Redis + 9 contract тестов, 6 feature-тестов присутствия, компонентные тесты пакета chat, `smoke websocket` и E2E двух пользователей | verified |
| notifications/offline-delivery | Уведомления тем, кого нет в комнате (presence-реестр как источник истины), категории и каналы с предпочтениями, группировка шумных событий, идемпотентные очереди с retry/backoff/failed и дайджест; push отправляется срочным с темой комнаты и по очереди живых уведомлений, подписка устройства восстанавливается сама (`pushsubscriptionchange` + сверка при запуске); лента, счётчик и настройки в интерфейсе. 19 package-тестов, 5 интеграционных, 7 компонентных, E2E `e2e messaging` (пропущенное сообщение доходит до ленты) | verified |
| search/message-search | Поиск по комнатам пользователя: индекс Typesense с безопасными полями, идемпотентная синхронизация после commit, права проверяются по PostgreSQL, документированная деградация 503, команда `chat:search-reindex`; лист поиска в шапке комнаты. Выключен по умолчанию. 11 package-тестов, 5 feature, 5 компонентных, `smoke search` против настоящего Typesense | verified |
| ai/file-summary | Пересказ приложенного документа по ответу с `@ai`: контракт `FileSummaryProvider` (Polza/Null/Fake), разбор PDF/DOCX/TXT внутри установки, окно 500–800 символов по границе предложения, очередь `ai` с уникальным ключом и backoff, событие `ai.file_summary.updated.v1` на личный канал и HTTP-ресинхронизация, черновик виден только запросившему и публикуется обычным сообщением от его имени по подтверждению; связка с перепиской — контрактами `SummarySource`/`SummaryPublisher` в composition root. 30 package-тестов пакета ai, 5 интеграционных, 10 компонентных, 3 адреса в OpenAPI; E2E `e2e ai-file-summary` выполнен на собранных образах в двух конфигурациях (помощник выключен и включён без ключа поставщика): документ доходит до второго браузера, ответ с `@ai` не превращается в сообщение комнаты, отказ поставщика объясняется словами и переписка продолжает работать. Успешный пересказ настоящим Polza в E2E не прогонялся — нужен `AI_API_KEY` | implemented |
| ai/text-revisions | Помощник за контрактом `TextRevisionProvider`: Polza (OpenAI-совместимый), Null и Fake; квоты, таймаут, повтор, circuit breaker; endpoint `/ai/message-revisions`; аудит без промптов и ответов; лист помощника в композере (принять/отклонить/отменить/вернуть). 20 package-тестов, 4 интеграционных, 6 компонентных, E2E `e2e ai-revision` | verified |
| administration/system-controls | Права `administration.*` и роль `super-admin` (spatie/laravel-permission без teams, ADR-010), `/admin/status`, выключатель AI в `system_settings` с применением на каждый запрос, журнал `audit_logs` с редактированием секретов и приватного текста, аудит AI-обращений, Horizon за gate; экран `/admin` в SPA; служебные консольные команды `admin:*` для оператора установки — первая `admin:reset-password` задаёт пользователю новый пароль на сервере (`chat:grant-admin` не изменилась). 12 package-тестов, 8 интеграционных, 4 компонентных, 3 feature | implemented |
| chat/direct-messages | Личная переписка один на один: начало по нику (`POST /direct-conversations`, `GET /direct-conversation-candidates`), один диалог на пару без оглядки на порядок людей, запрет диалога с собой, скрытие у себя (`POST /direct-conversations/{room}/hide`) с возвратом при новом сообщении; комнатные действия (переименование, архив, удаление, приглашение третьего, выход) отклоняются и запросом напрямую, остальные возможности и схемы событий — как в комнате; уведомление называет отправителя. Представление переписки сообщает `kind` и `counterpart`. 17 package-тестов, 13 feature, 6 компонентных; E2E `e2e direct-messages` написан, но ещё не выполнялся | implemented |
| presentation/product-landing-demo | Корневой `demo.html` — автономная премиальная code/programming-презентация в оранжево-графитово-зелёной палитре: один файл, пятнадцать зарегистрированных локальных сцен, человекоподобная печать/появление сообщений/реакция, ограниченный ambient-пул, русский декоративный вывод терминала при неизменной команде README и централизованные безопасные CTA-placeholder. GSAP+ScrollTrigger и Motion закреплены по версиям; reduced-motion, CDN/no-JS fallback и отсутствие продуктовых запросов сохраняются. Правило синхронизации — в `AGENTS.md`/`CLAUDE.md`; 12 browser-проверок в `apps/chat-web/e2e/demo-landing.spec.ts` компилируются, runtime smoke и визуальная приёмка редизайна отложены до поддерживаемого Chromium-окружения | implemented |

## Ограничения текущего состояния

- Сквозного шифрования нет: администратор сервера технически может прочитать
  переписку. Это заявленная граница MVP (`docs/security/threat-model.md`).
- Поиск и AI-помощник выключены по умолчанию; включение описано в
  `docs/operations/search-reindex.md`, `docs/features/ai-text-revision.md` и
  `docs/features/ai-file-summary.md`.
- Пересказ документа читает текстовый слой файла: сканы без текста и
  защищённые PDF отклоняются понятной ошибкой, распознавание изображений
  (OCR) не входит в MVP.
- Workflows написаны, но на GitHub Actions ещё не выполнялись: локально их
  шаги повторяет `./tools/chat ci` (он зелёный), а деплой-workflows требуют
  секретов и хостов конкретной установки.
- E2E-сценарий помощника проверяет выключенный путь; путь с настоящим
  провайдером включается `E2E_AI_ENABLED=true` и ключом поставщика.
- Скрытый диалог возвращается в список по перечитыванию списка (до 30 с), а не
  мгновенным событием: события приходят только по перепискам из списка.
- E2E личной переписки (`e2e direct-messages`) написан и включён в
  `e2e critical`, но ещё не выполнялся.
- Browser smoke и desktop/mobile/reduced-motion скриншоты редизайна
  `demo.html` не выполнены: Playwright Chromium недоступен в текущем окружении
  Ubuntu 26.04; тесты скомпилированы и ожидают поддерживаемого browser runner.
- Аудио- и видеозвонки, федерация и маркетплейс расширений не входят в MVP.
