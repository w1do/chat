## 1. Слой жестов

- [x] 1.1 Создать `packages/frontend/chat/src/hooks/useMessageGestures.ts` на Pointer Events (свайп влево, двойное касание, долгое нажатие) с порогами из design §1 и отменой жеста при вертикальном смещении; проверить компонентным тестом на каждый жест и на отказ при прокрутке (`./tools/chat web test chat`)
- [x] 1.2 Добавить в хук путь для указателя: правый клик открывает меню, двойной клик ставит быструю реакцию; проверить тестами, что стандартное меню браузера подавляется (скиллы: `spatie-javascript`)

## 2. Меню действий над сообщением

- [x] 2.1 Собрать `MessageActionsSheet` на `Sheet` из `@vendor/ui`: палитра реакций, «ответить», «копировать текст», «удалить» только для своих; проверить тестами состав меню для своего и чужого сообщения
- [x] 2.2 Реализовать копирование текста через Clipboard API с запасным путём и понятным тостом; проверить тестом успешного и неуспешного копирования
- [x] 2.3 Подключить меню к жестам и убрать колонку значков `↩ ☺ ✕` и инлайновую палитру из `ChatScreen`; проверить, что в ленте нет кнопок действий (тест на отсутствие) и что все действия доступны через меню

## 3. Доступность действий

- [x] 3.1 Сделать сообщение фокусируемым и открывать меню с клавиатуры (`Enter`, клавиша контекстного меню), навигация по пунктам стрелками; проверить клавиатурным тестом
- [x] 3.2 Проверить объявления для скринридера: автор, текст, время, реакции с количеством; закрепить тестом на accessible name сообщения и кнопки реакций

## 4. Лента по макету

- [x] 4.1 Перестроить группу сообщений: аватар и имя автора с обеих сторон, зеркальный порядок для своих, единый аватар на группу; проверить компонентными тестами для чужого, своего и группы подряд
- [x] 4.2 Обновить разделитель дня: дата в человекочитаемом виде и «Сегодня» для текущего дня; проверить тестом на обе ветки
- [x] 4.3 Привести пузыри, отступы, межстрочный интервал и цвет фона к макету, убрать цветную полоску-акцент; проверить визуально на узком экране и прогнать `./tools/chat web test chat`

## 5. «Печатает» в шапке

- [x] 5.1 Добавить чистую функцию `typingSummary(names, max)` и покрыть тестами случаи: один, двое, трое, четверо и больше, никто
- [x] 5.2 Перенести индикатор в шапку комнаты с фиксированной высотой строки и убрать его из ленты; проверить тестом, что лента не содержит индикатор, а шапка показывает нужную строку и уважает настройку «показывать печатает»

## 6. Клавиатура и безопасные зоны

- [x] 6.1 Добавить `interactive-widget=resizes-content` в `<meta viewport>` и вернуть `offsetTop` из `useKeyboardInsets`; проверить unit-тестом хука на изменение `visualViewport`
- [x] 6.2 Заякорить экран переписки к `visualViewport` (высота и сдвиг), сделать нижний отступ зависящим от состояния клавиатуры; проверить, что при открытой клавиатуре шапка видна, панель ввода над клавиатурой, а пустой полосы нет
- [ ] 6.3 Проверить вручную на iPhone (Safari) открытие и закрытие клавиатуры, поворот экрана и устройство с вырезом; записать результат в `docs/features/mobile-ui.md`

## 7. Экраны входа и регистрации

- [x] 7.1 Центрировать карточку формы в `LoginPage` (`100dvh`, центрирование, ограниченная ширина) и обновить заголовки/подписи; проверить компонентным тестом и вручную на узком экране
- [x] 7.2 Обновить поля и кнопки в `@vendor/identity`: подписи, подсказки, видимое состояние ошибки, кнопка на всю ширину; проверить `./tools/chat web test identity`
- [x] 7.3 Сохранять введённый логин при переключении вход/регистрация/восстановление; проверить тестом на переключение

## 8. Подсказка о жестах

- [x] 8.1 Показать однократную подсказку о свайпе и долгом нажатии при первом входе в комнату, с сохранением факта показа локально; проверить тестом, что подсказка появляется один раз и не мешает работе с лентой

## 9. Сценарии и документация

- [x] 9.1 Перевести E2E `messaging` и `realtime` на меню действий вместо исчезнувших кнопок; проверить `./tools/chat e2e critical`
- [x] 9.2 Обновить `docs/features/mobile-ui.md` (жесты, шапка с индикатором, поведение клавиатуры) и `docs/features/{replies,reactions}.md` в части взаимодействия; проверить `./tools/chat check docs`
- [x] 9.3 Обновить `CHANGELOG.md` и `SUMMARY.md`; финальная проверка `./tools/chat ci` и пересборка образа web
