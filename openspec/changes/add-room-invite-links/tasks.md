## 1. Приглашения в пакете chat

- [x] 1.1 Миграция `room_invites` (комната, автор, хэш токена, срок, отзыв, счётчик использований) и модель; проверить миграцию на чистой БД (`./tools/chat api migrate:fresh`) (скиллы: `architecture-ddd`, `spatie-laravel-php`)
- [x] 1.2 Реализовать `CreateInviteHandler` (только участник, не для архивной комнаты, случайный токен, в базе только хэш) и `RevokeInviteHandler`; проверить package-тестами: участник создаёт, посторонний получает отказ, архивная комната отказывает, отозванная ссылка не действует (скиллы: `architecture-ddd`)
- [x] 1.3 Реализовать `JoinByInviteHandler` (проверка срока и отзыва, вступление участником, идемпотентность повторного перехода, системная запись в ленту); проверить тестами: вступление, повтор без второй записи, просроченная и отозванная ссылка (скиллы: `architecture-ddd`)
- [x] 1.4 Endpoints `POST /rooms/{room}/invites`, `DELETE /invites/{invite}`, `GET /invites/{token}` (сведения о приглашении без утечки лишнего); проверить feature-тестами, включая 401 для гостя на создание и 404 на несуществующий токен

## 2. Аккаунт по приглашению

- [x] 2.1 Добавить `password_set_at` в `users` (nullable) и проставлять его при смене пароля пользователем; проверить тестами пакета identity, что у существующих аккаунтов поле пустое, а после смены пароля заполняется (скиллы: `spatie-laravel-php`)
- [x] 2.2 Реализовать создание аккаунта по имени: транслитерация логина с суффиксом, уникальность при коллизии, случайный пароль, почта пустая; проверить тестами: логин уникален и читаем, пароль не возвращается наружу (скиллы: `architecture-ddd`)
- [x] 2.3 Собрать сценарий в приложении: контроллер `/invites/{token}/accept` создаёт аккаунт (для гостя) и вступает в комнату в одной транзакции, вошедшему пользователю только вступление; проверить интеграционными тестами оба пути и откат при сбое (скиллы: `architecture-ddd`, `spatie-laravel-php`)
- [x] 2.4 Ограничить частоту: создание приглашений на пользователя, вступление на адрес; проверить тестом, что превышение даёт 429 с документированным телом (скиллы: `spatie-security`)

## 2а. Простые пароли

- [x] 2а.1 Снизить `identity.password.min_length` до 1 и синхронизировать схемы форм (`packages/frontend/identity/src/schemas/auth.ts`) с этим значением; проверить package-тестами: пароль из одного символа принимается при регистрации, смене и восстановлении, пустой — отклоняется (скиллы: `spatie-laravel-php`)
- [x] 2а.2 Убедиться, что ограничение частоты входов продолжает действовать, и описать в `docs/security/hardening.md`, как поднять минимальную длину; проверить тестом на 429 и `./tools/chat check docs` (скиллы: `spatie-security`, `documentation`)

## 3. Интерфейс

- [x] 3.1 Кнопка «Пригласить» в шапке комнаты рядом с поиском: запрашивает ссылку и кладёт в буфер готовый текст с названием комнаты, ссылкой и сроком; проверить компонентными тестами успешное копирование и запасной путь, когда буфер недоступен
- [x] 3.2 Экран `/invite/:hash`: показывает комнату и приглашающего, спрашивает имя, для вошедшего — только кнопку «Вступить»; проверить компонентными тестами оба состояния, а также просроченную и отозванную ссылку
- [x] 3.3 После вступления открывать нужную комнату; проверить тестом перехода
- [x] 3.4 Подсказка в настройках, пока пароль не задан: объяснить, что вход держится на сессии, и предложить задать логин и пароль; проверить тестом появление и исчезновение подсказки

## 4. Контракты, сценарии и документация

- [x] 4.1 Описать endpoints в OpenAPI, пересобрать dist и клиент; проверить `./tools/chat openapi validate` и `./tools/chat check boundaries`
- [x] 4.2 E2E: участник копирует приглашение, второй браузер открывает ссылку, называет имя и оказывается в комнате и пишет сообщение; проверить `./tools/chat e2e critical`
- [x] 4.3 Написать `docs/features/invite-links.md` (создание, срок, отзыв, границы доступа, аккаунт по приглашению), дополнить `docs/features/membership.md` и раздел о рисках в `docs/security/threat-model.md`; проверить `./tools/chat check docs` (скиллы: `documentation`)
- [x] 4.4 Обновить `CHANGELOG.md` и `SUMMARY.md`; финальная проверка `./tools/chat ci`, пересборка образов и `./tools/chat e2e critical`
