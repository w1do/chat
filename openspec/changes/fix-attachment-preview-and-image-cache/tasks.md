## 1. Миниатюра готова к моменту ответа

- [x] 1.1 Добавить порог `attachments.preview_sync_max_kb` в `packages/backend/chat/config/chat.php` (`ATTACHMENT_PREVIEW_SYNC_MAX_KB`, по умолчанию 4096) и в `.env.example`; проверить, что значение читается только из config, а не из `env()` в коде
- [x] 1.2 В `PreparesAttachmentPreviews` выбирать `nonQueued()`/`queued()` по размеру media; проверить package-тестом: файл в пределах порога даёт `hasGeneratedConversion('preview')` сразу после загрузки, файл больше порога — нет
- [x] 1.3 Проверить feature-тестом, что `POST /rooms/{room}/attachments` для обычной фотографии возвращает непустой `thumb_url`, а `GET /attachments/{uuid}/thumb` отвечает 200 без прогона очереди
- [x] 1.4 Проверить, что тяжёлое изображение по-прежнему уходит в очередь `media` и после её прогона `GET /messages/{message}` отдаёт непустой `thumb_url`

## 2. Догоняющий запрос миниатюры

- [x] 2.1 Добавить в `packages/frontend/chat` хук догоняющего запроса сообщения: до трёх попыток с возрастающими паузами, один запрос на сообщение независимо от числа плиток; проверить компонентным тестом на поддельном клиенте
- [x] 2.2 Подключить хук к плиткам вложений (`AttachmentTiles`): состояние ожидания сменяется изображением, как только `thumb_url` появился; проверить тестом, что перезагрузка приложения для этого не нужна
- [x] 2.3 После исчерпания попыток плитка объясняет, что миниатюра недоступна, остаётся нажимаемой и открывает оригинал; проверить тестом, что новых запросов не уходит
- [x] 2.4 Проверить тестом, что то же поведение работает для сообщения, пришедшего событием `message.created.v1` (получатель, а не отправитель)

## 3. Кеш изображений в service worker

- [x] 3.1 Завести кеш `chat-images-v1` в `apps/chat-web/public/sw.js`, перехватывать только GET изображений (миниатюры вложений, изображения профиля и комнат) по стратегии «сначала кеш»; проверить тестом service worker'а, что запросы API и навигация не затронуты
- [x] 3.2 Реализовать LRU-подрезку: попадание переписывает запись, подрезка удаляет самые давние сверх `IMAGE_CACHE_MAX_ENTRIES`; проверить тестом на переполнении
- [x] 3.3 Не кешировать ответы с ошибкой и ответы больше `IMAGE_CACHE_MAX_ENTRY_KB`; проверить тестом, что 404 неготовой миниатюры не оседает в кеше
- [x] 3.4 Отказ хранилища не ломает показ: при исключении в `cache.put` ответ всё равно возвращается; проверить тестом с падающим `caches`
- [x] 3.5 Поднять версию кеша оболочки и удалить прежние кеши на `activate`; проверить существующим тестом service worker'а

## 4. Прогрев ближайших изображений

- [x] 4.1 Добавить в `apps/chat-web` очередь прогрева с параллельностью 3, стартом по `requestIdleCallback` и памятью уже запрошенных адресов; проверить unit-тестом на пределы параллельности и отсутствие повторов
- [x] 4.2 Греть изображения комнат при открытии списка переписок и миниатюры последних сообщений при загрузке истории; проверить компонентным тестом, что адреса берутся из уже полученных данных и новых запросов к API не появляется
- [x] 4.3 Проверить тестом, что прогрев не задерживает отправку сообщения (очередь ограничена и не блокирует другие запросы)

## 5. Очистка кеша при смене человека

- [x] 5.1 Добавить команду `clear-images` в `app/service-worker.ts` и обработчик в `sw.js`; без установленного worker'а чистить `caches.delete()` из приложения — проверить тестом обе ветки
- [x] 5.2 Вызывать очистку при выходе и при переходе сессии в «истекла»; проверить тестом, что после выхода кеш пуст
- [x] 5.3 Хранить идентификатор последнего вошедшего локально и чистить кеш, когда `/me` вернул другого человека; проверить тестом сценарий общего устройства

## 6. Документация и приёмка

- [x] 6.1 Обновить `docs/features/attachments.md` (миниатюра без перезагрузки, порог, догоняющий запрос) и `docs/features/installable-app.md` (кеш изображений, пределы, очистка); проверить `./tools/chat check docs`
- [x] 6.2 Дописать `ATTACHMENT_PREVIEW_SYNC_MAX_KB` и пределы кеша в `docs/operations/configuration.md`, `CHANGELOG.md` и `SUMMARY.md`
- [x] 6.3 Прогнать `./tools/chat test chat`, `./tools/chat test api`, `./tools/chat web test` и `./tools/chat web test chat`; проверить, что все зелёные
- [x] 6.4 Проверить сценарий на собранных образах: отправить фотографию с телефона и убедиться, что плитка показывает изображение сразу, а повторное открытие переписки не идёт в сеть за миниатюрами
