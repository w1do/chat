## 0. Предпосылка

Хранилище, библиотека медиа, webp-конверсии и расширение обработки
изображений в образе приходят из `add-object-storage-foundation`.
Начинать это изменение до него бессмысленно: складывать файлы будет
некуда.

## 1. Вложения на сервере

- [x] 1.1 Подключить `spatie/laravel-medialibrary` к `packages/backend/chat`, объявить `Message` владельцем медиа с коллекцией `attachments` на диске `attachments`; проверить package-тестом, что файл уходит в хранилище под `uploads/`, а не на локальный диск
- [x] 1.2 Реализовать проверку принимаемого файла: белый список типов и расширений, отдельный запрет исполняемых, предел размера и количества из конфигурации; покрыть package-тестами подмену расширения, исполняемый файл, превышение размера и превышение количества
- [x] 1.3 Добавить загрузку вложения отдельным адресом в границах комнаты под правом отправки сообщения и ограничением частоты; проверить feature-тестами: участник загружает (201), посторонний — отказ, неверный тип — 422
- [x] 1.4 Разрешить отправку сообщения со списком идентификаторов вложений и снять безусловную обязательность текста: сообщение полно, если есть текст или вложение; проверить feature-тестами «только вложение», «ни текста, ни вложений» (422), «чужое вложение не прикрепляется»
- [x] 1.5 Объявить очередную конверсию-миниатюру для изображений в отдельной очереди; проверить тестом, что конверсия ставится в очередь, а до её готовности адрес миниатюры отсутствует
- [x] 1.6 Добавить выдачу файла и миниатюры через приложение с политикой чтения сообщения и `Content-Disposition: attachment`; проверить feature-тестами: участник получает файл, посторонний — отказ, исключённый — отказ, вложение удалённого сообщения — отказ
- [x] 1.7 Удалять файлы вместе с комнатой (включая миниатюры) и не терять уборку при недоступном хранилище; проверить package-тестом удаление файлов комнаты и сохранность файлов соседней комнаты
- [x] 1.8 Убирать по расписанию вложения, загруженные, но так и не отправленные, старше суток; проверить package-тестом, что отправленные вложения уборка не трогает

## 2. Контракт

- [x] 2.1 Добавить вложения в представление сообщения (идентификатор, имя, тип, размер, адрес файла, адрес миниатюры и размеры изображения); пустой список у сообщения без вложений, отсутствие списка у удалённого; проверить feature-тестом
- [x] 2.2 Расширить схему `message.created.v1` в `packages/contracts` теми же полями без смены версии и проверить `./tools/chat contracts validate` и contract-тест схем
- [x] 2.3 Обновить OpenAPI (загрузка вложения, выдача файла, вложения в сообщении), пересобрать dist и клиент; проверить `./tools/chat openapi validate` и `./tools/chat check boundaries`

## 3. Интерфейс

- [x] 3.1 Добавить скрепку в панель ввода: выбор нескольких файлов за раз, предпросмотр плитками, снятие лишнего до отправки, ход загрузки и ошибка по каждому файлу отдельно; проверить компонентными тестами выбор нескольких файлов, снятие одного, повтор неудавшегося и сохранность черновика
- [x] 3.2 Показать вложения в сообщении: одно изображение крупно, два-четыре сеткой, больше четырёх — четыре плитки и «Показать ещё»; не-изображения строкой с именем, размером и скачиванием; проверить компонентными тестами каждый случай, включая смешанное сообщение
- [x] 3.3 Грузить в ленте миниатюры, а не оригиналы, и показывать состояние ожидания, пока миниатюра готовится; закрепить компонентным тестом
- [x] 3.4 Добавить галерею: открытие по нажатию на плитку, листание свайпом и стрелками, Esc закрывает, возврат на то же место ленты; проверить компонентными тестами открытие нужного изображения, края галереи и клавиатуру
- [x] 3.5 Показать вложения из события `message.created.v1` без перезапроса истории; проверить компонентным тестом на real-time событии

## 4. Проверки и документация

- [x] 4.1 E2E: человек прикладывает несколько изображений и документ, второй браузер видит плитки и открывает галерею; проверить `./tools/chat e2e critical`
- [x] 4.2 Написать `docs/features/attachments.md` и обновить `docs/features/messaging.md` (сообщение без текста, удаление уносит файлы); проверить `./tools/chat check docs`
- [x] 4.3 Дополнить `docs/security/threat-model.md`: приём файлов, белый список типов, отказ исполняемым, выдача вложением
- [x] 4.4 Обновить `CHANGELOG.md` и `SUMMARY.md`; финальная проверка `./tools/chat ci`
