## 1. Оболочка владеет экраном

- [x] 1.1 Запретить прокрутку документа в `apps/chat-web/src/styles/index.css` (`overflow: hidden`, `overscroll-behavior: none`, высота 100% у `html`/`body`/`#root`); проверить в браузере, что `window.scrollY` не меняется при жестах у края
- [x] 1.2 Добавить примитив `Screen` в `@vendor/ui` (grid `auto / minmax(0,1fr) / auto`, середина с `.scroll-area`, необязательные шапка и низ); покрыть компонентным тестом: одна область прокрутки, шапка и низ вне неё, необязательный низ не занимает места

## 2. Экран переписки

- [x] 2.1 Перевести `ChatScreen` на `Screen`: шапка, лента, панель ввода; убрать абсолютное позиционирование панели и ручной расчёт `paddingBottom`; проверить `./tools/chat web test chat`
- [x] 2.2 Проверить, что панель ввода видна целиком и при закрытой, и при открытой клавиатуре, а лента продолжает прокручиваться внутри себя; закрепить компонентным тестом на структуру и вручную на телефоне
- [x] 2.3 Сохранить автопрокрутку к новому сообщению и переход к цитате; закрепить тестами (приход сообщения прокручивает ленту, а не интерфейс)

## 3. Остальные экраны

- [x] 3.1 Перевести `RoomsScreen` на `Screen` (шапка, список, нижняя навигация остаются на месте); проверить компонентным тестом и вручную при одной комнате — прокрутки быть не должно
- [x] 3.2 Перевести `SettingsScreen`, `NotificationsPage`, `RoomSettingsPage`, `AdminPage` на `Screen`, убрать `min-height: 100dvh`; проверить `./tools/chat web test` и `web test chat`
- [x] 3.3 Перевести `LoginPage`: карточка центрируется внутри прокручиваемой середины и остаётся доступной на низком экране с открытой клавиатурой; проверить компонентным тестом

## 4. Десктоп

- [x] 4.1 Сделать колонки независимыми оболочками (`height: 100%`, `min-width: 0`, прокрутка в середине каждой); проверить, что при длинном списке комнат и короткой переписке двигается только левая колонка
- [x] 4.2 Проверить изменение размера окна: приложение занимает новую видимую область без полос прокрутки страницы; закрепить E2E-проверкой

## 5. Проверки и документация

- [x] 5.1 Добавить в E2E-сценарий проверку неподвижности страницы: после жестов прокрутки в ленте и списке комнат `window.scrollY` равен нулю, а лента прокрутилась; проверить `./tools/chat e2e critical`
- [ ] 5.2 Проверить вручную на iPhone и Android: отсутствие резинового отскока, панель ввода целиком, поведение при открытой клавиатуре и повороте; записать результат в `docs/features/mobile-ui.md`
- [x] 5.3 Обновить `docs/features/mobile-ui.md` (правила оболочки и исчезновение pull-to-refresh на Android) и `docs/features/installable-app.md` в части десктопа; проверить `./tools/chat check docs`
- [x] 5.4 Обновить `CHANGELOG.md` и `SUMMARY.md`; финальная проверка `./tools/chat ci`, пересборка образа web и `./tools/chat e2e critical`
