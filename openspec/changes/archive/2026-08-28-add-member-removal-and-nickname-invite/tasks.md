## 1. Исключение участника на сервере

- [x] 1.1 Добавить в `MembershipPolicy` правило `remove`: владелец — любого, кроме себя; админ — только роль `member`; остальные — никого. Расширить package-тест матрицы ролей всеми четырьмя случаями
- [x] 1.2 Реализовать удаление участия одной операцией с проверкой, что комната не остаётся без владельца; проверить package-тестом, что участие исчезает, а сообщения исключённого остаются
- [x] 1.3 Добавить `DELETE /rooms/{room}/members/{member}`; проверить feature-тестами: владелец исключает (204), админ исключает обычного участника (204), админ получает 403 на владельце и на другом админе, участник — 403, посторонний — 404
- [x] 1.4 Транслировать `room.member_changed.v1` с `action: "removed"` после коммита и записать системную запись в историю комнаты, как при вступлении; проверить тестом на событие и на запись
- [x] 1.5 Проверить feature-тестом, что исключённый больше не читает комнату и не пишет в неё

## 2. Поиск людей по нику

- [x] 2.1 Реализовать поиск кандидатов: очистка ведущего `@`, минимум два символа, совпадение с начала ника без учёта регистра, не больше десяти результатов, отметка «уже в комнате»; покрыть package-тестами все правила, включая пустой и слишком короткий запрос
- [x] 2.2 Добавить `GET /rooms/{room}/member-candidates` под правом `invite` и тем же ограничением частоты, что у приглашений; проверить feature-тестами: владелец и админ получают результат, участник и посторонний — отказ, ответ содержит только идентификатор, ник и имя
- [x] 2.3 Обновить OpenAPI (удаление участника и поиск кандидатов), пересобрать dist и клиент; проверить `./tools/chat openapi validate` и `./tools/chat check boundaries`

## 3. Интерфейс

- [x] 3.1 Заменить поле «ID пользователя» на поиск по нику: ввод `@ник`, список найденных с именем, добавление нажатием; проверить компонентными тестами ввод с собакой и без, отсутствие поиска короче двух символов, сообщение «никого не нашли» с предложением позвать ссылкой
- [x] 3.2 Пометить в результатах тех, кто уже в комнате, и не давать пригласить их повторно; закрепить компонентным тестом
- [x] 3.3 Добавить в строку участника действие «Исключить» с подтверждением, видимое владельцу и админу по тем же правилам, что и на сервере; проверить тестами: владелец видит действие у всех кроме себя, админ — только у обычных участников, участник не видит его вовсе
- [x] 3.4 Обработать `room.member_changed.v1` с `action: "removed"` про себя: комната закрывается, человек уходит к списку комнат с сообщением, комната пропадает из списка; проверить компонентным тестом

## 4. Проверки и документация

- [x] 4.1 E2E: владелец приглашает второго человека по нику и затем исключает его; второй браузер с открытой комнатой оказывается в списке комнат; проверить `./tools/chat e2e critical`
- [x] 4.2 Обновить `docs/features/membership.md` (кто кого исключает, приглашение по нику, что остаётся после исключения) и `docs/api/`, если там описан состав комнаты; проверить `./tools/chat check docs`
- [x] 4.3 Обновить `CHANGELOG.md` и `SUMMARY.md`; финальная проверка `./tools/chat ci`
