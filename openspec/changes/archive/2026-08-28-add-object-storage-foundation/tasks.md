## 1. Хранилище в составе установки

- [x] 1.1 Вывести хранилище из профиля `s3` в основной набор сервисов `infra/compose/compose.prod.yaml` и `compose.dev.yaml` и добавить его в корневой `docker-compose.yml`; проверить `./tools/chat compose config`
- [x] 1.2 Передать переменные хранилища в окружение контейнера `api` и описать их в `.env.example` с комментариями; проверить, что приложение поднимается с ними и что без них ошибка понятна
- [x] 1.3 Добавить расширение обработки изображений в `infra/docker/api/Dockerfile`; проверить `./tools/chat build images` и тем, что расширение видно в собранном образе

## 2. Приложение видит хранилище

- [x] 2.1 Завести закрытый диск поверх бакета `chat` с префиксами по видам медиа (`uploads/`, `avatars/`, `rooms/`, `wallpapers/`); проверить тестом, что диск приватный и что запись идёт под нужным префиксом
- [x] 2.2 Создавать бакет при запуске идемпотентно: «уже есть» — успех, а не сбой; проверить тестом оба случая и вручную повторным `docker compose up` на существующих данных
- [x] 2.3 Подключить `spatie/laravel-medialibrary`: зависимость в пакетах-владельцах медиа, регистрация провайдера и миграции приложением; проверить миграцию на чистой PostgreSQL и `./tools/chat check boundaries`
- [x] 2.4 Задать общие правила медиа — диск по умолчанию, отдельная очередь конверсий, webp как формат производных; проверить тестом, что объявленная конверсия ставится в очередь и даёт webp

## 3. Готовность и проверка

- [x] 3.1 Добавить `ComponentCheck` для хранилища рядом с `DatabaseCheck`/`RedisCheck` и включить его в readiness; покрыть тестом оба исхода без утечки адресов и ключей в ответ
- [x] 3.2 Добавить `./tools/chat smoke storage`: записать объект, прочитать обратно, сверить содержимое, удалить за собой; проверить на поднятом стеке успех и понятный отказ при отсутствии прав записи
- [x] 3.3 Включить дымовую проверку хранилища в `./tools/chat smoke all` и убедиться, что она не оставляет мусора в бакете

## 4. Документация

- [x] 4.1 Записать ADR о переводе объектного хранилища в обязательные компоненты и поправить `CLAUDE.md` §14, где оно названо опциональным
- [x] 4.2 Обновить `docs/operations/installation.md` (хранилище в составе, переменные, дымовая проверка), `upgrade.md` (добавление сервиса в существующую установку) и `backup-restore.md` (файлы наравне с базой)
- [x] 4.3 Дополнить `docs/security/threat-model.md`: закрытый бакет, выдача файлов только приложением, содержимое не исполняется браузером
- [x] 4.4 Обновить `CHANGELOG.md` (с пометкой BREAKING про обязательное хранилище) и `SUMMARY.md`; финальная проверка `./tools/chat ci`
