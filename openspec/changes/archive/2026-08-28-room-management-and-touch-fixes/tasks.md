## 1. Права и удаление в пакете chat

- [x] 1.1 Разделить политику комнаты: `update` (название и описание) — владелец и админ, `delete` — только владелец, `archive` остаётся у владельца и админа; проверить package-тестами все три роли и постороннего (скиллы: `architecture-ddd`, `laravel-permission-development`)
- [x] 1.2 Реализовать `DeleteRoomHandler`: в одной транзакции удалить реакции, сообщения, приглашения, участие и комнату; проверить тестами, что после удаления не остаётся ни одной связанной записи и что удаление идемпотентно при повторе (скиллы: `architecture-ddd`)
- [x] 1.3 Перевести `DELETE /rooms/{room}` на удаление навсегда, вынести архивирование в `POST /rooms/{room}/archive`; проверить feature-тестами: владелец удаляет (204), админ получает 403, посторонний 404, архивирование по новому адресу продолжает работать
- [x] 1.4 Проверить `PATCH /rooms/{room}` на новые правила: непустое название, описание может быть пустым, админ меняет, `member` получает 403; закрепить feature-тестами
- [x] 1.5 Транслировать `room.deleted.v1` в канал комнаты после коммита и записать удаление в журнал аудита; проверить тестом на событие и запись (скиллы: `architecture-ddd`, `laravel-activitylog`)

## 2. Управление комнатой в интерфейсе

- [x] 2.1 Добавить в `RoomSettingsPage` форму названия и описания (react-hook-form + zod), видимую только владельцу и админу; проверить компонентными тестами: сохранение, ошибка пустого названия, отсутствие формы у `member`
- [x] 2.2 Добавить удаление комнаты с подтверждением вводом названия, видимое только владельцу; проверить тестами: кнопка неактивна до совпадения, после удаления переход к списку комнат
- [x] 2.3 Обработать `room.deleted.v1` на клиенте: открытая комната закрывается, человек попадает в список комнат с сообщением, комната исчезает из списка; проверить компонентным тестом
- [x] 2.4 Проверить, что после переименования название обновляется в шапке комнаты и в списке без перезагрузки; закрепить тестом

## 3. Долгое нажатие без выделения

- [x] 3.1 Вынести запрет выделения в класс общих стилей (`user-select`, `-webkit-user-select`, `-webkit-touch-callout: none`) и применить его к пузырю сообщения и слою меню действий; явно вернуть `user-select: text` полям ввода и поиску; проверить компонентными тестами наличие класса и `./tools/chat web test chat`
- [ ] 3.2 Проверить на iPhone и Android: долгое нажатие открывает меню без синей подсветки и без системного меню, «Копировать текст» по-прежнему кладёт текст в буфер; записать результат в `docs/features/mobile-ui.md`

## 4. Низ экрана в видимой области

- [x] 4.1 Дополнить `useKeyboardInsets` измерением нижней полосы (`innerHeight - visualViewport.height - offsetTop`, ниже порога клавиатуры) с запасным путём на `env(safe-area-inset-bottom)`; покрыть unit-тестом на оба случая и на отсутствие `visualViewport` (скиллы: `spatie-javascript`)
- [x] 4.2 Отдавать величину в CSS-переменную `--app-bottom-inset` на корне приложения и считать по ней отступ панели ввода и нижней навигации; убрать расчёт по одной лишь безопасной зоне; проверить компонентными тестами и `./tools/chat web test`
- [x] 4.3 Убедиться, что панель ввода занимает всю ширину и при открытой клавиатуре стоит вплотную над ней; закрепить компонентным тестом на структуру
- [ ] 4.4 Проверить вручную на iPhone и Android: панель ввода целиком видна при закрытой и открытой клавиатуре, с системной полосой навигации по полям, после поворота и при скрытии адресной строки; записать результат в `docs/features/mobile-ui.md`

## 5. Контракты, сценарии и документация

- [x] 5.1 Обновить OpenAPI (удаление, архивирование, изменение комнаты) и схему события `room.deleted.v1`, пересобрать dist и клиент; проверить `./tools/chat openapi validate` и `./tools/chat check boundaries`
- [x] 5.2 E2E: владелец переименовывает комнату и удаляет её, второй браузер с открытой комнатой оказывается в списке комнат; проверить `./tools/chat e2e critical`
- [x] 5.3 Обновить `docs/features/rooms.md` (управление комнатой, кто что может, необратимость удаления), `docs/features/mobile-ui.md` и `docs/operations/backup-restore.md` (удалённую комнату вернёт только резервная копия); проверить `./tools/chat check docs` (скиллы: `documentation`)
- [x] 5.4 Обновить `CHANGELOG.md` (пометить перенос архивирования как BREAKING) и `SUMMARY.md`; финальная проверка `./tools/chat ci`, пересборка образов и `./tools/chat e2e critical`
