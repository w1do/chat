## 1. Срочная доставка push

- [x] 1.1 Добавить в `WebPushTransport` явные `urgency` и `topic` при отправке (тема — ключ схлопывания уведомления по комнате и категории); проверить package-тестом на fake-транспорте, что параметры доходят до отправки, и `./tools/chat test packages`
- [x] 1.2 Перевести `Category::Message` из очереди `notifications-bulk` в `notifications`, оставив bulk рассылкам; закрепить unit-тестом на соответствие категорий очередям и проверить, что группы Horizon покрывают обе очереди
- [x] 1.3 Проверить feature/интеграционным тестом, что уведомление о новом сообщении ставится в очередь `notifications` и уходит в push с высокой срочностью; `./tools/chat test api`

## 2. Самовосстановление подписки

- [x] 2.1 Отдавать публичный ключ VAPID и минимальную длину пароля в `/config.json` (шаблон web-контейнера и `runtime-config.ts`), не пересобирая образ; проверить компонентным тестом чтения конфигурации и `docker compose config`
- [x] 2.2 Добавить в `public/sw.js` обработчик `pushsubscriptionchange`: переподписаться ключом из `/config.json` и отправить новую подписку на сервер; покрыть тестом service worker (событие → повторная подписка → запрос к серверу)
- [x] 2.3 При запуске приложения отправлять имеющуюся подписку на сервер повторно и выводить состояние переключателя из ответа; проверить компонентными тестами: сервер принял — «включено», сервер ответил ошибкой — «выключено», подписки в браузере нет — «выключено»
- [x] 2.4 Проверить, что повторная отправка той же подписки не создаёт дубликат на сервере (идемпотентность по `endpoint`); закрепить feature-тестом

## 3. Понятные сообщения об ошибках

- [x] 3.1 Заменить коды `validation.*` человеческим текстом в схемах `packages/frontend/identity` и `packages/frontend/chat`; проверить компонентными тестами форм входа, регистрации, профиля, почты, пароля и создания комнаты
- [x] 3.2 Добавить тест, который обходит схемы форм и падает, если сообщение выглядит как служебный код (начинается с `validation.`); проверить `./tools/chat web test`

## 4. Требование к паролю одно на всех

- [x] 4.1 Строить схемы с паролем как функции от минимальной длины из runtime-конфигурации (регистрация, смена пароля, восстановление); проверить тестами на двух значениях: умолчание установки и поднятое значение
- [x] 4.2 Показывать действующее число в подсказке рядом с полем и в тексте ошибки вместо жёстко вписанных «10 символов»; закрепить компонентным тестом
- [x] 4.3 Проверить сквозным тестом, что пароль, который принимает сервер, принимает и форма: смена пароля на короткий проходит при умолчании установки

## 5. Проверка, контракты и документация

- [ ] 5.1 Проверить вручную на iPhone и Android: push при спящем телефоне приходит сразу, уведомления по одной комнате не копятся, после перевыпуска подписки доставка восстанавливается сама; записать результат в `docs/features/web-push.md`
- [x] 5.2 Обновить `docs/features/web-push.md` (срочность, очередь, самовосстановление подписки, честное состояние переключателя) и `docs/features/authentication.md` (сообщения об ошибках и откуда берётся длина пароля); проверить `./tools/chat check docs`
- [x] 5.3 Привести `docs/security/hardening.md` в соответствие с поведением: интерфейс действительно берёт `PASSWORD_MIN_LENGTH` из конфигурации установки; проверить `./tools/chat check docs`
- [x] 5.4 Обновить `CHANGELOG.md` и `SUMMARY.md`; финальная проверка `./tools/chat ci` и `./tools/chat e2e critical`
