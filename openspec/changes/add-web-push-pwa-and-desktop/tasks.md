## 1. Инфраструктура Web Push

- [x] 1.1 Установить библиотеку Web Push в `apps/chat-api` через контейнер composer с сетью, зафиксировать версию в `composer.lock`; проверить, что образ api собирается офлайн (`./tools/chat build images`) (скиллы: `laravel-deploy`)
- [x] 1.2 Добавить конфигурацию VAPID в пакет `notifications` (`config/notifications.php`) и команду `chat:push-keys` для генерации пары ключей; проверить тестом, что при пустых ключах push считается выключенным, и запуском команды (скиллы: `spatie-laravel-php`)

## 2. Подписки устройств

- [x] 2.1 Миграция `push_subscriptions` (уникальный endpoint, ключи, владелец, `last_used_at`) и модель в пакете `notifications`; проверить миграцию на чистой БД (`./tools/chat api migrate:fresh`) (скиллы: `architecture-ddd`)
- [x] 2.2 Реализовать use cases подписки и отписки (`SubscribeDevice`, `UnsubscribeDevice`) и endpoints `POST /push-subscriptions`, `DELETE /push-subscriptions`; проверить тестами: первая подписка, повторная тем же endpoint без дубля, отписка, отказ на чужую подписку, 401 для гостя (скиллы: `architecture-ddd`, `laravel-permission-development`)

## 3. Доставка push

- [x] 3.1 Добавить `Channel::Push` и включить его в предпочтения категорий со значениями по умолчанию; проверить тестами пакета, что канал виден в предпочтениях и что обязательной остаётся только лента
- [x] 3.2 Реализовать `WebPushSender` (подпись VAPID, шифрование, короткий фрагмент текста, ссылка на комнату) и ветку канала в `DeliverNotificationJob`; проверить тестами: содержимое без секретов, обрезка длинного текста, идемпотентность задания
- [x] 3.3 Обрабатывать ответы push-сервиса: `404`/`410` удаляют подписку, прочие ошибки уходят в `failed()` без влияния на сообщение; проверить тестами на оба случая
- [x] 3.4 Проверить сквозные правила: получателю в комнате push не уходит, инициатору не уходит, группировка не плодит уведомления; интеграционные тесты в `apps/chat-api`

## 4. Service worker и установка

- [x] 4.1 Добавить манифест и иконки в `apps/chat-web`, подключить его в `index.html`; проверить, что браузер считает приложение устанавливаемым (Lighthouse installability или `chrome://flags` вручную) и что `./tools/chat web build` кладёт файлы в сборку
- [x] 4.2 Написать service worker: обработка `push` и `notificationclick` (открыть нужную комнату, переиспользовать окно), network-first для навигации, оффлайн-заглушка; проверить unit-тестами обработчиков и вручную в браузере
- [x] 4.3 Реализовать обновление версии: сообщение приложению о готовом service worker и применение по подтверждению; проверить, что после выкладки новой сборки старые файлы не используются
- [x] 4.4 Настроить отдачу `manifest.webmanifest` и `sw.js` в nginx образа web (без кеша для service worker); проверить заголовки `curl -I` на собранном образе

## 5. Подписка и тумблер в интерфейсе

- [x] 5.1 Реализовать в `apps/chat-web` подписку на push: запрос разрешения, регистрация service worker, отправка подписки на сервер, снятие при выключении; проверить компонентными тестами на успешный и отклонённый путь
- [x] 5.2 Добавить в настройки тумблер «Push-уведомления» с объяснением особых случаев (разрешение запрещено, не поддерживается, на iPhone нужна установка); проверить тестами каждое состояние
- [x] 5.3 Показать одноразовую подсказку об установке приложения, запомнив ответ; проверить тестом, что подсказка появляется один раз

## 6. Раскладка для компьютера

- [x] 6.1 Добавить `useMediaQuery` в `@vendor/ui` и покрыть unit-тестом смены ширины
- [x] 6.2 Реализовать две колонки в `ChatPage` на ширине от 1024 px (список слева, переписка справа, приглашение выбрать комнату); проверить компонентными тестами обе раскладки и сохранение открытой комнаты при сужении окна
- [x] 6.3 Проверить клавиатурную работу на десктопе: фокус в списке комнат, открытие комнаты, отправка по `Enter`; закрепить тестом

## 7. Сценарии, документация и выкладка

- [x] 7.1 Добавить E2E-сценарий десктопной раскладки (широкий viewport: список и переписка одновременно) и включить его в `./tools/chat e2e critical`
- [ ] 7.2 Проверить push вручную на телефоне (установленное приложение, закрытый чат) и записать результат; при отсутствии устройства — зафиксировать как непроверенное
- [x] 7.3 Обновить `docs/features/notifications.md` (канал push, приватность содержимого), добавить `docs/features/installable-app.md` и раздел о десктопе в `docs/features/mobile-ui.md`, дополнить `docs/operations/configuration.md` и `docs/security/secret-rotation.md` ключами VAPID; проверить `./tools/chat check docs` (скиллы: `documentation`)
- [x] 7.4 Обновить `CHANGELOG.md`, `SUMMARY.md`, `.env.example` и compose (переменные VAPID); финальная проверка `./tools/chat ci`, пересборка образов и `./tools/chat e2e critical`
