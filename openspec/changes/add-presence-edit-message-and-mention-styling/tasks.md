## 1. Отслеживание присутствия и времени активности (Backend)

- [x] 1.1 Добавить миграцию с полем `last_seen_at` в таблицу `users` и обновить модель `User`; проверить выполнение миграции через `./tools/chat api test`
- [x] 1.2 Реализовать middleware/сервис обновления `last_seen_at` с троттлингом через Redis при активности пользователя; проверить unit/feature тестом
- [x] 1.3 Добавить поля `is_online` и `last_seen_at` в API-ресурсы `UserResource`, `MemberResource` и схемы OpenAPI; проверить тестом контрактов

## 2. Статусы присутствия и форматирование времени (Frontend)

- [x] 2.1 Создать утилиту `formatLastSeen` для локализованного вывода времени последней активности («В сети», «был(а) в сети только что / N мин назад / вчера»); покрыть unit-тестами
- [x] 2.2 Интегрировать отображение онлайна и last seen в шапку чата/диалога, список участников `MemberRow` и индикатор `PresenceDots`; проверить компонентными тестами

## 3. Редактирование сообщений (Backend & Contracts)

- [x] 3.1 Проверить и дополнить `PATCH /api/v1/messages/{message}`, `EditMessageRequest`, `EditMessageHandler` и отправку broadcast-события `MessageUpdatedV1`; проверить Feature-тестом на успешное редактирование и запрет редактирования чужих сообщений
- [x] 3.2 Добавить `is_edited` и `edited_at` в `MessageData` и `MessageResource`; обновить OpenAPI и сгенерированный клиент `@vendor/api-client`

## 4. Редактирование сообщений (Frontend UI & Realtime)

- [x] 4.1 Добавить пункт «Редактировать» в меню действий `MessageActionsSheet` для собственных сообщений; проверить компонентным тестом
- [x] 4.2 Реализовать режим редактирования в `ChatScreen`: подстановка текста в строку ввода, плашка с индикатором редактируемого сообщения и кнопкой отмены; проверить тестами взаимодействия
- [x] 4.3 Добавить обработку события `message.updated.v1` в realtime-подписках `useMessages`/`handlers.ts` и отображение метки `(отредактировано)` в `MessageBubble`; проверить тестами

## 5. Интерактивное автодополнение упоминаний (Mention Picker & Styling)

- [x] 5.1 Переработать `MentionPicker.tsx`: стилизованное всплывающее меню над полем ввода с подсветкой аватарок, цветным акцентным кольцом, статусом присутствия, никнеймом и именем; проверить компонентным тестом
- [x] 5.2 Реализовать клавиатурную навигацию (`ArrowUp`, `ArrowDown`, `Enter`, `Tab`, `Escape`) и корректную вставку `@username ` в `ChatScreen`; проверить тестами
- [x] 5.3 Реализовать стилизацию бейджей упоминаний в `MessageBubble` и подсветку фона сообщения при упоминании текущего пользователя; проверить тестами

## 6. Верификация, документация и лендинг

- [x] 6.1 Запустить полный набор тестов бэкенда и фронтенда (`./tools/chat api test`, `./tools/chat web test`); проверить отсутствие ошибок и регрессий
- [x] 6.2 Обновить документацию `docs/features/rooms.md`, `CHANGELOG.md` и демонстрационный лендинг `demo.html`
