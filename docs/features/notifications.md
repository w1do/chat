# Уведомления

Статус: verified (E2E `./tools/chat e2e messaging`)

## Главное правило

**Уведомление не создаётся тому, кто сейчас в этой комнате.** Активность
берётся из Redis-реестра присутствия пакета `chat` через контракт
`ActivityInspector`; связывает пакеты приложение
(`app/Providers/PackageWiringProvider.php`), сами пакеты друг о друге не знают.

Инициатор события не уведомляется никогда — в том числе если упомянул себя.

## Категории и каналы

| Категория | Что это | Лента | Почта | Push | Очередь |
|---|---|---|---|---|---|
| `message` | новое сообщение в комнате | вкл | выкл | вкл | `notifications-bulk` |
| `mention` | вас упомянули | вкл | вкл | вкл | `notifications` |
| `room_invite` | вас добавили в комнату | вкл | вкл | вкл | `notifications` |
| `security` | безопасность и администрирование | **всегда вкл** | вкл | вкл | `notifications-critical` |

Пользователь меняет каналы в настройках («Каналы уведомлений»). Ленту для
категории «Безопасность» отключить нельзя — попытка возвращает 422.

## Группировка

Повторяющиеся события одной комнаты внутри окна обновляют одну запись и
увеличивают `group_count` вместо создания новых. Окна: сообщения — 15 минут,
упоминания — 5 минут (`NOTIFY_GROUP_*_SECONDS`).

## Доставка

Медленные каналы уходят в очередь: `DeliverNotificationJob` идемпотентен
(уникальный ключ «получатель + канал + повод»), имеет `tries`, `backoff`,
`timeout` и `failed()`. Провал письма пишется в журнал и **не влияет** ни на
сохранённое сообщение, ни на real-time доставку. `SendDigestJob` шлёт сводку
непрочитанного одним письмом. Аккаунтам без почты (вход по логину, ADR-005)
письма не отправляются.

Цепочка таймаутов соблюдается: job timeout (30 с) < Horizon supervisor timeout
< queue `retry_after` (см. `docs/operations/supervisor.md`).

## Push-уведомления на устройство

Push доходят, когда чат закрыт: сервер отправляет их через Web Push с ключами
VAPID напрямую в push-сервис браузера — без Firebase и любого внешнего
control plane.

Правила те же, что и у остальных каналов: не тому, кто сейчас в комнате, не
инициатору, с группировкой шумных комнат. В уведомление идёт комната, автор и
короткий фрагмент — ровно то, что человек и так увидит в ленте; длинный текст
обрезается. **Это видно на экране блокировки** — учитывайте при обсуждении
чувствительного.

Включение на сервере:

```bash
docker compose exec api php artisan chat:push-keys   # ключи VAPID
# впишите VAPID_* в .env и перезапустите стек
```

Без ключей push просто выключены: канал не предлагается, остальные работают.

Проверить доставку, не гадая:

```bash
docker compose exec api php artisan chat:push-test <логин>
```

Команда скажет ровно одно из: push выключены (нет ключей), у пользователя нет
подписанных устройств, push-сервис отклонил отправку (с причиной) или
«Отправлено устройств: N».

### Очереди

Push уходит через очередь категории (`notifications`, `notifications-bulk`,
`notifications-critical`). В Docker их обрабатывает контейнер `worker` с
Horizon — запускать ничего вручную не нужно. На установке без Docker очереди
поднимает Supervisor (`infra/supervisor/horizon.conf`).

Важно при обновлении: контейнеры `worker`, `scheduler` и `reverb` должны быть
пересобраны вместе с `api`. Worker со старым кодом не сможет разобрать задание,
созданное новым, и доставка будет падать.

Пользователь управляет push одним тумблером в настройках: включение
запрашивает разрешение браузера и подписывает устройство, выключение снимает
подписку — системные уведомления не приходят вовсе. Лента и письма при этом
продолжают работать по предпочтениям категорий.

Устройства, которые push-сервис объявил недействительными (404/410),
удаляются автоматически. Прочие ошибки уходят в повтор очереди и **не влияют**
на отправку сообщения.

### Срочность и очередь

Push уходит с заголовком `Urgency: high`. Без него push-сервис считает
уведомление обычным и не будит спящее устройство — на Android оно тогда ждёт,
пока человек сам возьмёт телефон в руки. Всё, что мы отправляем в push, —
повод посмотреть в телефон, поэтому срочность одна для всех категорий.

Вместе со срочностью отправляется `Topic` — отпечаток того же ключа, по
которому уведомления схлопываются на устройстве (категория и комната). Пока
прошлое уведомление о комнате не доставлено, push-сервис заменит его новым, а
не накопит очередь устаревших.

Уведомление о новом сообщении идёт по очереди `notifications`, вместе с
упоминаниями и приглашениями. Очередь `notifications-bulk` оставлена
рассылкам — там живёт только дайджест: живой разговор не должен стоять за
массовой отправкой.

### Подписка восстанавливается сама

Браузер перевыпускает и отзывает подписку сам — Chrome на Android делает это
регулярно. Возврат в рассылку работает двумя независимыми путями:

- service worker слушает `pushsubscriptionchange`: подписывается заново ключом
  из `/config.json` (приложение в этот момент может быть закрыто) и отправляет
  новую подписку на сервер;
- приложение при каждом запуске отправляет свою подписку повторно. Запрос
  идемпотентен по `endpoint`, поэтому это и починка на случай пропущенного
  события, и честный ответ на вопрос, знает ли сервер об устройстве.

Поэтому переключатель push показывает «включено» только тогда, когда подписку
подтвердил сервер, а не когда она просто есть у браузера. Раньше отвалившееся
устройство выглядело подписанным и молчало.

На iPhone push работают только у установленного приложения — см.
`docs/features/installable-app.md`.

### Проверка на устройствах

Доставку push нельзя подтвердить ни тестом, ни эмулятором: решение принимает
push-сервис браузера. Проверка на живом телефоне — обязательный шаг: приход
уведомления при спящем экране, отсутствие очереди устаревших уведомлений по
одной комнате, восстановление доставки после перевыпуска подписки.

| Дата | Устройство | Приход при спящем экране | Восстановление подписки |
|---|---|---|---|
| — | iPhone / Safari (установленное) | не проверено | не проверено |
| — | Android / Chrome | не проверено | не проверено |

## API

`GET /notifications`, `POST /notifications/read`,
`GET|PATCH /notification-preferences`, `POST|DELETE /push-subscriptions` —
см. OpenAPI dist.

## Интерфейс

- Колокольчик с точкой непрочитанного в шапке списка чатов.
- Экран «Уведомления»: лента с категориями, счётчиком свёрнутых событий
  (`×4`), переходом в комнату и кнопкой «Отметить всё прочитанным»;
  состояния loading/empty/error.
- Настройки → «Каналы уведомлений»: переключатели по категориям и каналам,
  заблокированные строки объясняют, почему их нельзя выключить.

## Проверки

- `./tools/chat test notifications` — 19 тестов: подавление активного
  получателя и инициатора, группировка внутри окна и новая запись за его
  пределами, очереди по категориям, предпочтения, идемпотентность заданий,
  отказ почты, дайджест;
- `./tools/chat test api tests/Integration/NotificationsWiringTest.php` —
  сквозной путь chat → notifications (5 тестов);
- `./tools/chat web test notifications` — лента и настройки (7 тестов).

Запуск E2E: `./tools/chat e2e messaging` — сообщение, отправленное, пока получателя
нет в комнате, доходит до его ленты уведомлений.
